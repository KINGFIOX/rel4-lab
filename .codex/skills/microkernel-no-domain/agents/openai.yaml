interface:
  display_name: "Microkernel No Domain"
  short_description: "Keep domain scheduling out of scope"
  default_prompt: "Use $microkernel-no-domain when rolling back or changing this microkernel so seL4 domain scheduling, domain time slices, domain queues, and domain-aware scheduling decisions are removed or kept out of scope unless explicitly requested."
