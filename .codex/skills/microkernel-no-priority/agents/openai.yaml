interface:
  display_name: "Microkernel No Priority"
  short_description: "Ignore priority for scheduling"
  default_prompt: "Use $microkernel-no-priority when changing this microkernel so seL4-compatible priority APIs may exist but rel4 scheduling stays round-robin and priority-insensitive; user-space must not rely on priority for correctness, ordering, or progress unless explicitly requested."
