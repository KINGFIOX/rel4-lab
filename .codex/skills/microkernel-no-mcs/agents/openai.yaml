interface:
  display_name: "Microkernel No MCS"
  short_description: "Keep MCS real-time scheduling out of scope"
  default_prompt: "Use $microkernel-no-mcs when changing this microkernel so MCS real-time scheduling, sched-context budget enforcement, timeout-fault budget delivery, refill queues, and budget donation stay absent or kept out of scope unless explicitly requested."
