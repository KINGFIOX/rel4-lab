interface:
  display_name: "Microkernel Has No MCS"
  short_description: "Do not add MCS names or stubs to first-party sources"
  default_prompt: "Use $microkernel-no-mcs when changing this microkernel. The kernel has no MCS surface; do not add MCS names, constants, comments, stubs, or non-MCS contrasts to first-party sources."
